#include "main.h"

/**
  *print_last_digit -prints last digit of anumber
  *@n: the number that wil be printed the last digit 
  *Return: last digit
  */
int print_last_digit(int n)
{
	return (n % 10);
}
